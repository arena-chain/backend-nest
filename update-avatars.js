const mongoose = require("mongoose"); mongoose.connect("mongodb://localhost:27017/arenachain").then(async () => { const result = await mongoose.connection.db.collection("users").updateMany({ avatar: { $regex: /svg\?seed/ } }, [{ $set: { avatar: { $replaceOne: { input: "$avatar", find: "svg?seed", replacement: "png?seed" } } } }]); console.log("Updated avatars:", result.modifiedCount); process.exit(); })
